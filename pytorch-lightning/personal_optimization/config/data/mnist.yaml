# general
preproc:
  train_val_split:
    ratio: 0.8
    seed: 42

train:
  _target_: datasets.MNISTDataSet
  root: MNIST
  download: true
  train: true

test:
  _target_: datasets.MNISTDataSet
  root: MNIST
  download: true
  train: false