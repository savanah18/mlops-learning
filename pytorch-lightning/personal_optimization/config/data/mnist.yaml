# general
preproc:
  train_val_split:
    ratio: 0.8
    seed: 42

datamodule:
  _target_: datamodules.mnist.MNISTDataModule
  data_dir: "../data"
  batch_size: 32
  num_workers: 16
  train_val_split_ratio: ${..preproc.train_val_split.ratio}
  train_val_split_seed: ${..preproc.train_val_split.seed}